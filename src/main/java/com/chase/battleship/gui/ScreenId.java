package com.chase.battleship.gui;

public enum ScreenId {
        TITLE,
        SINGLE_PLAYER_SELECT,
        MULTI_PLAYER_SELECT,
        HOST_MODE_SELECT,
        HOST_LOBBY,
        JOIN_CODE,
        SETUP,
        PLAYING,
        WIN,
	LOSE,
        DISCONNECTED
,
        CONTROLS
}
